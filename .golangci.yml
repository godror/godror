linters-settings:
  maligned:
    suggest-new: true

linters:
  disable:
    - megacheck
    - errcheck
    - typecheck

issues:
  exclude:
    - "`?encodeFixed(32|64)Pb`? is unused"

# vim: set et shiftwidth=2:
